{ %opt=-O4 -CriotR }

{$mode objfpc}

function f : integer;
  begin
    result:=1234;
  end;

var r, s, t, tablecols : integer;
    
begin
  s := 1;
  t := r + (s*tablecols);
  t := r + (s*tablecols);
end.
